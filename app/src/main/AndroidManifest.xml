<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ueo.licentaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RegisterActivity"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".activities.PageActivity"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".activities.ForgotPassword"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".activities.ChooseBarber"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".activities.PickTimeAndDate"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".activities.ChoosePlan"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".activities.AccountInformation"
            tools:ignore="Instantiatable" />
    </application>

</manifest>